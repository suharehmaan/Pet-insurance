import React from "react";
import { Typography, Box } from "@mui/material";

const Home = () => {
  return (
    <Box>
      <Typography variant="h4">Welcome to Pet Insurance Dashboard</Typography>
      <Typography>Manage your policies, claims, and more from here.</Typography>
    </Box>
  );
};

export default Home;
